一篇论文 PDF → 中文 L3 精读笔记。等同 `/paper-reading`。

Read and follow `{{SKILL_DIR}}/SKILL.md` exactly.

Treat **the PDF, path, or DOI given after `/精读`** (or the attached file) as the article.

Default: L3 精读. `润色` plus a named note → polish only. `L2` → L2. Skip preprints unless named. Same DOI already in `NOTES_DIR` → stop and say so.

Start now.
