一篇论文 PDF → 中文 L3 精读笔记（导读、抽图、摘抄、判断）。别名 /精读。

Read and follow `{{SKILL_DIR}}/SKILL.md` exactly.

Treat **the PDF, path, or DOI given after `/paper-reading`** (or the attached file) as the article.

Default: L3 精读. If the text after the command is `润色` plus a named note, polish only. If it is `L2`, do L2. Skip preprints unless named. Same DOI already in `NOTES_DIR` → stop and say so.

Start now.
